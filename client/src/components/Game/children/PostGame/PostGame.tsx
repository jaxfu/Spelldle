import styles from "./PostGame.module.scss";

const PostGame: React.FC = () => {
	return (
		<div className={styles.root}>
			<div className={styles.content}>PostGame</div>
		</div>
	);
};

export default PostGame;
